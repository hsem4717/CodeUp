#include<stdio.h>
int main()
{
    int num, sum, i, j;
    scanf("%d",&num);
    for(i = 1; i<=9; i++)
    {
        sum = i*num;
        for(j = 1; j<=sum;j++){
            printf("*");
        }
        printf("\n");
        
    }
    return 0;
}
