# include <stdio.h>

int main()
{
	int a,b,c,m;
	scanf("%d %d %d",&a,&b,&c);
	if(a > b)
		m = a;
	else
		m = b;
	if(c > m)
		m = c;
	if(m < (a+b+c)-m)
		printf("yes");
	else
		printf("no");
		
	return 0;
}
